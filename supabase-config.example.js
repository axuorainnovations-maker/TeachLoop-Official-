// Copy this file to `supabase-config.js` and fill in the two values from your
// Supabase project: Dashboard → Project Settings → API.
//   • Project URL  → SUPABASE_URL
//   • anon public key → SUPABASE_ANON_KEY
// Both are safe to expose in the browser (the anon key is designed to be public;
// your data is protected by Row Level Security policies — see SUPABASE_SETUP.md).
//
// `supabase-config.js` is gitignored so your keys never get committed.
window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
